<% if content_for?(:header) %>
  <div class="bg-[#EF4444] flex justify-center">
    <div class="max-w-7xl px-4 py-4 flex flex-1 justify-between items-end">
      <%= yield :header %>
    </div>
  </div>
<% end %>
